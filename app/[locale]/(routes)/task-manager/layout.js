export const metadata = {
  title: "Task Manager — DoIt",
  description: "Manage all your objectives in a kanban-style task board.",
};

export default function TaskManagerLayout({ children }) {
  return children;
}

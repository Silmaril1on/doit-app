export const metadata = {
  title: "Generate Assets — DoIt",
  description: "Generate custom badge and avatar assets for your DoIt profile.",
};

export default function GenerateAssetsLayout({ children }) {
  return children;
}
